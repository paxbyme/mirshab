services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      # Compose ichida postgres/redis xizmatlariga ulanish
      DB_URL: postgresql+asyncpg://mirshab:${POSTGRES_PASSWORD:-mirshab}@db:5432/mirshab
      REDIS_URL: redis://redis:6379/0
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    volumes:
      - ./logs:/app/logs

  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: mirshab
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-mirshab}
      POSTGRES_DB: mirshab
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mirshab"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--save", "", "--appendonly", "no"]

volumes:
  pgdata:

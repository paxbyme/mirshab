# --- Core ---
aiogram==3.13.1
sqlalchemy[asyncio]==2.0.35
aiosqlite==0.20.0
asyncpg==0.29.0
alembic==1.13.3
pydantic-settings==2.5.2
loguru==0.7.2
redis==5.0.8
apscheduler==3.10.4
python-dotenv==1.0.1

# --- AI moderatsiya (Claude) — profilga jalb qiluvchi spam-botlarni aniqlash ---
# ANTHROPIC_API_KEY berilmasa bot heuristikaga fallback qiladi (majburiy emas).
anthropic>=0.40.0

# --- Faza 7 (opsional) ---
# Quyidagilar bo'lmasa ham bot ishlaydi (graceful fallback).
# Web dashboard:
fastapi==0.115.0
uvicorn[standard]==0.31.0
jinja2==3.1.4

# Rasm NSFW detection (NudeNet) va OCR — og'ir paketlar, alohida o'rnatiladi:
#   pip install -r requirements-ml.txt
